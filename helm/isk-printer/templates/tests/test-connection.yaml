apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isk-printer.fullname" . }}-test-connection"
  labels:
{{ include "isk-printer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "isk-printer.fullname" . }}:{{ .Values.test.service.port }}']
  restartPolicy: Never
